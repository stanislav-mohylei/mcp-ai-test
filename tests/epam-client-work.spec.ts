import { test, expect } from '@playwright/test';

test.describe('EPAM site - Client Work navigation', () => {
  test('navigate from home -> services -> client work', async ({ page }) => {
    // TEST_BODY_PLACEHOLDER
  });
});
