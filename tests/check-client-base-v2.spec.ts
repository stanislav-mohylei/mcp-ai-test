import { test, expect } from '@playwright/test';

test.describe('Check client base v2', () => {
  test('should navigate to client work', async ({ page }) => {
    /* TEST_STEPS_PLACEHOLDER */
  });
});
